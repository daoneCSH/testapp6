CREATE TABLE TB_USER (
     id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
     userid VARCHAR2(255 CHAR) NOT NULL UNIQUE,
     password VARCHAR2(255 CHAR) NOT NULL,
     testtext VARCHAR2(255 CHAR) NOT NULL
);